import Box from "@mui/material/Box";
import Header from "../../components/layout/navbar/Header.tsx";


export default function SettingsPage() {

    return (
        <Box>
            <Header past={ [] } current={ "Settings" } />

        </Box>
    );
}
